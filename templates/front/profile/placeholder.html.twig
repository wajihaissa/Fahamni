{% extends 'front/base_front.html.twig' %}

{% block title %}Profil – FAHIMNI{% endblock %}
{% block current_page %}profile{% endblock %}

{% block content %}
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm border-0 rounded-3 p-4">
                <h1 class="h4 mb-3">Profil utilisateur</h1>
                <p class="text-muted mb-0">
                    Page profil (module user à intégrer). ID utilisateur : <strong>{{ user_id }}</strong>.
                </p>
                <p class="mt-3 mb-0 small text-muted">
                    <a href="{{ path('app_messenger_index') }}">Retour au Messenger</a>
                </p>
            </div>
        </div>
    </div>
</div>
{% endblock %}
