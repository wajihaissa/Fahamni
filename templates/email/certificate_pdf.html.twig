<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Helvetica', Arial, sans-serif;
            background: #f0f2f5;
        }
        .certificate {
            width: 100%;
            height: 100%;
            background: white;
            border: 20px solid #4361ee;
            padding: 40px;
            text-align: center;
            box-sizing: border-box;
        }
        h1 {
            font-size: 48px;
            color: #4361ee;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        .subtitle {
            font-size: 24px;
            color: #7209b7;
            margin-bottom: 40px;
        }
        .recipient {
            font-size: 42px;
            font-weight: bold;
            color: #2b2d42;
            margin: 30px 0;
            border-bottom: 3px solid #4361ee;
            display: inline-block;
            padding-bottom: 10px;
        }
        .quiz-title {
            font-size: 32px;
            color: #4361ee;
            margin: 30px 0;
        }
        .score {
            font-size: 28px;
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            margin: 30px auto;
            max-width: 300px;
        }
        .date {
            font-size: 18px;
            color: #6c757d;
            margin-top: 50px;
        }
        .seal {
            margin-top: 30px;
            font-size: 20px;
            color: #4361ee;
        }
    </style>
</head>
<body>
    <div class="certificate">
        <h1>CERTIFICATE OF ACHIEVEMENT</h1>
        <div class="subtitle">This is proudly presented to</div>
        <div class="recipient">{{ user.fullName }}</div>
        <div class="quiz-title">for successfully completing the</div>
        <div class="quiz-title" style="font-weight: bold;">{{ quiz.titre }}</div>
        <div class="score">Score: {{ percentage }}%</div>
        <div class="date">Awarded on {{ date|date('F j, Y') }}</div>
        <div class="seal">FAHIMNI Learning Platform</div>
    </div>
</body>
</html>