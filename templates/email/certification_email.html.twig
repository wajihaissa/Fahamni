<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: 'Segoe UI', sans-serif; background: #f4f7fc; margin: 0; padding: 20px; }
        .container { max-width: 600px; margin: 0 auto; background: white; border-radius: 16px; box-shadow: 0 10px 30px rgba(0,0,0,0.1); overflow: hidden; }
        .header { background: linear-gradient(135deg, #4361ee, #7209b7); padding: 30px; text-align: center; }
        .header h1 { color: white; margin: 0; font-size: 28px; }
        .badge { width: 80px; height: 80px; background: white; border-radius: 50%; margin: 0 auto 20px; display: flex; align-items: center; justify-content: center; }
        .badge svg { width: 50px; height: 50px; fill: #4361ee; }
        .content { padding: 40px; text-align: center; }
        .name { font-size: 32px; font-weight: 700; color: #2b2d42; margin: 10px 0; }
        .quiz-title { font-size: 24px; color: #4361ee; margin: 10px 0; }
        .score { background: #f8f9fa; border-radius: 50px; padding: 15px; margin: 30px 0; }
        .score-number { font-size: 48px; font-weight: 700; color: #4361ee; }
        .passed { background: #4cc9f0; color: white; padding: 15px; border-radius: 50px; font-size: 18px; font-weight: 600; }
        .footer { background: #f8f9fa; padding: 20px; text-align: center; color: #6c757d; }
        .btn { display: inline-block; padding: 12px 30px; background: #4361ee; color: white; text-decoration: none; border-radius: 8px; margin-top: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="badge">
                <svg viewBox="0 0 24 24"><path d="M12 2L2 7v8c0 5 10 7 10 7s10-2 10-7V7l-10-5z"/></svg>
            </div>
            <h1>Certification Achieved! 🎉</h1>
        </div>
        <div class="content">
            <h2>Congratulations,</h2>
            <div class="name">{{ user.fullName }}</div>
            <p>You have successfully passed the</p>
            <div class="quiz-title">{{ quiz.titre }}</div>
            <div class="score">
                <div>Your Score</div>
                <div class="score-number">{{ percentage }}%</div>
            </div>
            <div class="passed">✅ CERTIFIED</div>
            <p>Your certificate is attached to this email.</p>
            <a href="#" class="btn">View Certificate</a>
        </div>
        <div class="footer">
            <p>© {{ "now"|date('Y') }} FAHIMNI Learning Platform. All rights reserved.</p>
        </div>
    </div>
</body>
</html>